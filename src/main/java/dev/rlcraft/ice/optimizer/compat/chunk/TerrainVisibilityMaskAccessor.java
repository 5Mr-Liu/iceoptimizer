package dev.rlcraft.ice.optimizer.compat.chunk;

/** Early-CoreMod ABI for SetVisibility's synchronized 36-bit mirror. */
public interface TerrainVisibilityMaskAccessor {
    long ice$visibilityMask();
}
